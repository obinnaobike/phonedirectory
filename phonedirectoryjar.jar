

import java.io.*;
import java.util.*;





public class phonedirectoryjar {
	
	public static void main(String[] args) throws FileNotFoundException {
		
		Class jar = new Class("/Users/user/Desktop/write.txt");
		jar.display();
		ArrayList List = new ArrayList();
		Scanner input = new Scanner(System.in);
		
		
		For(int i=0;i<5;i++)
		{System.out.println("add data”)
			String line = input.nextLine();
			String name;
			String phonenumber;
			if(line == “y”)
			{
				System.out.println("Enter name");
				name = input.nextLine();
				System.out.println("Enter number");
			phonenumber = input.nextLine();
				ArrayList.insert(name, phonenumber);
				ArrayList.addtofile();
				
			}
			
			ArrayList.insert("Jordan","212432122");
		
		    ArrayList.insert("Anthony","209335321");
		
		    ArrayList.insert("Rokebread", "1204532264");
		    
		    ArrayList.insert("Ha", "410456107");
			ArrayList.add();
			
			System.out.println("delete data from file”)			line = input.nextLine();
			if(line == ‘y’)
			{
				System.out.println("Enter name ");
				name = input.nextLine();
				ArrayList.remove(name);
				ArrayList.addfile();
				
			}
			System.out.println("look up number"); 
			line = input.nextLine();
			if(line == ‘y’)
			{
				System.out.println("Enter name");
				name = input.nextLine();
				System.out.println(name);
				ArrayList.find("andy");
				
				
			}
			
			System.out.println("change data”)
			line = input.nextLine();
			if(line == “y”)
			{
				System.out.println("Enter name");
				name = input.nextLine();
				System.out.println("Enter number ");
				phonenumber = input.nextLine();
				ArrayList.find (name, phonenumber); 
				ArrayList.add();
				
			}
			
		    
			System.out.println("would you like to see what is in the file");
			line = input.nextLine();
			if(line == “y"))
			{
				 String filename = "/Users/user/Desktop/write.txt"; // this part reads from the file
				 
				 File textfils = new File(filename);
				 
				 Scanner in = new Scanner(textFils);
				 
				 
				 
				 while(in.hasNextLine()) {
					 
					 String lin105 = in.nextLine();
					 
					 System.out.print( lin105 ); 
					 
					 
					 
				 
				 
		           }	     
				 in.close();
			}
		
			System.out.println("display arraylist");
			line = input.nextLine();
			if(line == “yes”
			{
				
				ArrayList.display();// displays to screen
				
			}
			

			System.out.println("exit");
			line = input.nextLine();
			if(line == “y”)
			{
				
				break;
				
			}
			
			
		
			
		}	
			
	   }			
	}		
		    
		    
			
		
		



